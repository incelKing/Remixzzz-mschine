# Add project specific ProGuard rules here.
# Minification is currently disabled for the release build type in this
# early scaffold (see app/build.gradle.kts), so this file is not yet
# exercised -- kept here so the reference in build.gradle.kts resolves.
